# Configuration Docker pour Flask Test Server
# Copier ce fichier vers .env et ajuster les valeurs selon vos besoins

# ===== Configuration Flask =====
FLASK_ENV=production
DEBUG=False

# ===== Configuration Nginx =====
# Port exposé par Nginx (accessible depuis l'hôte)
NGINX_PORT=8080

# ===== Configuration Base de Données =====
# Chemin de la base de données dans le conteneur
DB_PATH=/app/data/test.db

# ===== Configuration Réseau =====
# Nom du réseau Docker (optionnel)
NETWORK_NAME=app-network

# ===== Configuration des Volumes =====
# Nom du volume pour la base de données (optionnel)
DB_VOLUME_NAME=db-data

# ===== Configuration Gunicorn =====
# Nombre de workers (recommandé: 2-4 x nombre de CPU cores)
GUNICORN_WORKERS=2
# Nombre de threads par worker
GUNICORN_THREADS=2
# Timeout des requêtes (en secondes)
GUNICORN_TIMEOUT=60

# ===== Configuration Logs =====
# Niveau de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ===== Notes =====
# - Ne pas commiter le fichier .env dans Git
# - Les valeurs par défaut sont définies dans docker-compose.yml
# - Redémarrer les conteneurs après modification : docker-compose restart